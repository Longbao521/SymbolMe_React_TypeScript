export interface InfoType {
    id: string;
    key: string;
    value: string;
}

export interface SkillInfo {
    id: string;
    html: string;
}

export interface TagInfo {
    id: string;
    text: string;
    color: string;
}